// TODO: Improve routing to handle whether user is authenticated with Google
//  or not, although maybe we can just do this with props and local storage

export const routeCodes = {
    HOME: '/',
    ABOUT: '/about',
};